{
  "name": "companybrain",
  "version": "0.1.0",
  "private": true,
  "description": "Team operating system that watches every channel and never lets a loop slip.",
  "license": "MIT",
  "packageManager": "pnpm@9.15.9",
  "engines": { "node": ">=20" },
  "workspaces": ["packages/*", "apps/*", "cli"],
  "scripts": {
    "dev": "tsx cli/bin/companybrain.ts",
    "start": "tsx cli/bin/companybrain.ts",
    "api": "tsx apps/api/src/main.ts",
    "typecheck": "tsc -b",
    "test": "node --test"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
